{
  "name": "img-gen-cli",
  "version": "1.0.1",
  "description": "AI Image Generation CLI - Generate images using Flux, DALL-E, Imagen, and more",
  "type": "module",
  "bin": {
    "generate": "./dist/cli.js"
  },
  "scripts": {
    "build": "bun build ./src/cli.ts --outdir ./dist --target node",
    "dev": "bun run ./src/cli.ts",
    "test": "bun test",
    "lint": "eslint src/",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "commander": "^12.1.0",
    "openai": "^4.77.0",
    "replicate": "^1.0.1",
    "@google/genai": "^1.0.0",
    "chalk": "^5.3.0",
    "ora": "^8.1.1",
    "sharp": "^0.33.5"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "typescript": "^5.7.2"
  }
}
